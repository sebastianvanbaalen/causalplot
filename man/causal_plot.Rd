% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_plot.R
\name{causal_plot}
\alias{causal_plot}
\title{Create a causal plot}
\usage{
causal_plot(
  labels = NULL,
  type = "1111",
  fill_variables = "grey80",
  fill_mechanisms = "grey90",
  corner_radius = 0.12,
  font = "sans",
  text_size = 4,
  text_color = "black",
  wrap_width = 24,
  arrow_length = grid::unit(3, "mm"),
  arrow_linewidth = 0.6,
  box_ratio = 0.8,
  xlim = NULL,
  ylim = NULL
)
}
\arguments{
\item{labels}{Character vector of labels. If NULL, defaults are used.}

\item{type}{Character. Layout template.}

\item{fill_variables}{Fill color for the first and last box (variables).}

\item{fill_mechanisms}{Fill color for the in-between boxes (mechanisms).}

\item{corner_radius}{Rounded corner radius. Either a grid::unit() or a numeric (interpreted as cm).}

\item{font}{Font family for labels (e.g., "sans", "serif"). Default is "sans".}

\item{text_size}{Numeric label size.}

\item{text_color}{Label color.}

\item{wrap_width}{Integer. Approximate wrap width for labels (in characters).}

\item{arrow_length}{grid::unit for arrowhead size.}

\item{arrow_linewidth}{Numeric line width for arrows.}

\item{box_ratio}{Numeric. Passed to coord_fixed(ratio = box_ratio). Default 0.8.}

\item{xlim}{Numeric vector of length 2, or NULL for auto-computed limits.}

\item{ylim}{Numeric vector of length 2, or NULL for auto-computed limits.}
}
\value{
A ggplot object.
}
\description{
Draw a simple causal diagram using ggplot2, with rounded boxes and arrows.
}
\details{
Supported templates:
\itemize{
\item "111" : IV -> mech -> DV (3 boxes)
\item "1111": IV -> mech1 -> mech2 -> DV (4 boxes)
\item "11111": IV -> mech1 -> mech2 -> mech3 -> DV (4 boxes)
\item "1121": IV -> one box -> two boxes -> DV (5 boxes)
\item "1211": IV (center) -> two boxes -> one box -> DV (5 boxes)
\item "1221": IV (center) -> two parallel paths -> DV (center) (6 boxes)
\item "bathtub": like "1221" but only bottom path + dashed direct IV->DV (4 boxes)
\item "111_moderator": like "111" but with a moderator variable
\item "111_confounder": like "111" but with a confounder variable
\item "211" : two IVs -> shared mechanism -> DV (4 boxes)
\item "221" : two IVs -> two parallel mechanisms -> DV (5 boxes)
\item "2221": two IVs -> two parallel mechanism steps -> DV (7 boxes)
}

Labels are wrapped to a maximum of 3 lines. If a label would wrap to more than
3 lines (given \code{wrap_width}), an error is thrown.
}
