% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/typology.R
\name{typology}
\alias{typology}
\title{Create a 2x2 typology diagram}
\usage{
typology(
  type_labels = c("Type 1", "Type 2", "Type 3", "Type 4"),
  x_axis_label = "Dimension 2",
  y_axis_label = "Dimension 1",
  x_axis_values = c("Low", "High"),
  y_axis_values = c("Low", "High"),
  fill = "grey90",
  border_color = NA,
  line_color = "grey50",
  line_linewidth = 0.5,
  corner_radius = 0.12,
  font = "sans",
  text_size = 4,
  text_color = "black",
  axis_text_size = 3.5,
  wrap_width = 20,
  arrow_length = grid::unit(2, "mm"),
  arrow_linewidth = 0.5,
  box_ratio = 0.8,
  xlim = NULL,
  ylim = NULL
)
}
\arguments{
\item{type_labels}{Character vector of length 4. Labels for each quadrant,
in order: top-left, top-right, bottom-left, bottom-right.
Default: \code{c("Type 1", "Type 2", "Type 3", "Type 4")}.}

\item{x_axis_label}{Character. Label for the horizontal axis (top).
Default: \code{"Dimension 2"}.}

\item{y_axis_label}{Character. Label for the vertical axis (left side).
Default: \code{"Dimension 1"}.}

\item{x_axis_values}{Character vector of length 2. Labels for the left (low)
and right (high) ends of the x-axis. Default: \code{c("Low", "High")}.}

\item{y_axis_values}{Character vector of length 2. Labels for the top
and bottom ends of the y-axis. Default: \code{c("Low", "High")}.}

\item{fill}{Fill color for the outer box. Default: \code{"grey90"}.}

\item{border_color}{Color of the outer box border. Default: \code{NA} (no border).}

\item{line_color}{Color of the dashed dividing lines. Default: \code{"grey50"}.}

\item{line_linewidth}{Numeric. Line width for the dashed dividing lines. Default: \code{0.5}.}

\item{corner_radius}{Rounded corner radius. Either a grid::unit() or a numeric
(interpreted as cm). Default: \code{0.12}.}

\item{font}{Font family for labels (e.g., "sans", "serif"). Default: \code{"sans"}.}

\item{text_size}{Numeric. Text size for type labels. Default: \code{4}.}

\item{text_color}{Color for all text labels. Default: \code{"black"}.}

\item{axis_text_size}{Numeric. Text size for axis labels and endpoint values. Default: \code{3.5}.}

\item{wrap_width}{Integer. Approximate wrap width for type labels (in characters). Default: \code{20}.}

\item{arrow_length}{grid::unit for arrowhead size. Default: \code{grid::unit(2, "mm")}.}

\item{arrow_linewidth}{Numeric line width for axis arrows. Default: \code{0.5}.}

\item{box_ratio}{Numeric. Passed to coord_fixed(ratio = box_ratio). Default: \code{0.8}.}

\item{xlim}{Numeric vector of length 2, or NULL for auto-computed limits.}

\item{ylim}{Numeric vector of length 2, or NULL for auto-computed limits.}
}
\value{
A ggplot object.
}
\description{
Draw a 2x2 typology diagram with four labeled quadrants, double-headed
axis arrows, and axis labels using ggplot2. The diagram consists of a single
rounded outer box divided into four quadrants by dashed midlines, with
double-headed arrows indicating the two dimensions.
}
\details{
The \code{type_labels} vector maps to quadrants as follows:
\itemize{
\item \code{type_labels[1]}: top-left (high y-axis, low x-axis)
\item \code{type_labels[2]}: top-right (high y-axis, high x-axis)
\item \code{type_labels[3]}: bottom-left (low y-axis, low x-axis)
\item \code{type_labels[4]}: bottom-right (low y-axis, high x-axis)
}
}
\examples{
# Basic typology with defaults
typology()

# Custom labels
typology(
  type_labels = c("Foxes", "Eagles", "Mice", "Snakes"),
  x_axis_label = "Speed",
  y_axis_label = "Size",
  x_axis_values = c("Slow", "Fast"),
  y_axis_values = c("Small", "Large")
)
}
